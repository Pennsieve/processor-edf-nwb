data_interfaces:
  edf: EDFRecordingInterface

metadata:
  NWBFile:
    data_collection: '${SESSION_TIME_PROVENANCE}'

experiments:
  experiment:
    sessions:
      - nwbfile_name: ${OUTPUT_FILE}
        source_data:
          edf:
            file_path: ${INPUT_FILE}
        conversion_options:
          edf:
            data_representation: ${DATA_REPRESENTATION}
